# note: codeberg actions (.forgejo/workflows/) is now the primary ci/cd platform
# this gitlab ci configuration is kept as a backup/mirror
# see .forgejo/workflows/validate.yml and .forgejo/workflows/release.yml for active pipelines

include:
  - project: simonoscr/pipeline-templates
    file: release/semantic-release.gitlab-ci.yml
